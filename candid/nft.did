type nft_canister = record {
    principal_id: principal;
    name: text;
    standard: text;
};

service : {
    "name" : () -> (text) query;

    "add" : (nft_canister) -> (text);
    "get_all" : () -> (vec nft_canister);
    "remove" : (text) -> (text);
}