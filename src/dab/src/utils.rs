fn binary_search(list: BTreeMap, target: Principal, low: u64, high: u64) {
    let keys: Vec<_> = list.keys().cloned().collect();
    
    //if (target == &keys[high]) {
    //    &keys[high]
    //} else if (target == &keys)
}